To copy builder write in console:

1) git init

2) git remote add origin https://github.com/alexandrcook/gulp-builder.git

3) git pull origin master

4) git remote rm origin

OR just Download .zip and unpack to you project folder


To start developing write in console:

1) npm install

//Wait until everything is established

2) gulp

Enjoy!!!